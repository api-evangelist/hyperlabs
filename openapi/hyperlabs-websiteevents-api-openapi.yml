openapi: 3.2.0
info:
  title: Hyperlabs.Web Website Events API
  version: '1.0'
servers:
- url: /api
tags:
- name: WebsiteEvents
paths:
  /v1/website/events:
    get:
      tags:
      - WebsiteEvents
      responses:
        '200':
          description: OK
      summary: Get website events
      x-summary-source: derived
      operationId: getV1WebsiteEvents
      x-operation-id-source: derived
components:
  securitySchemes:
    oauth2:
      type: apiKey
      description: 'Standard Authorization header using the Bearer scheme. Example: "Bearer {token}"'
      name: Authorization
      in: header
