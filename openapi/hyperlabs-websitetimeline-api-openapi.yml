openapi: 3.2.0
info:
  title: Hyperlabs.Web Website Timeline API
  version: '1.0'
servers:
- url: /api
tags:
- name: WebsiteTimeline
paths:
  /v1/website/timeline:
    get:
      tags:
      - WebsiteTimeline
      responses:
        '200':
          description: OK
      summary: Get website timeline
      x-summary-source: derived
      operationId: getV1WebsiteTimeline
      x-operation-id-source: derived
components:
  securitySchemes:
    oauth2:
      type: apiKey
      description: 'Standard Authorization header using the Bearer scheme. Example: "Bearer {token}"'
      name: Authorization
      in: header
