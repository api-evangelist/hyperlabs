openapi: 3.2.0
info:
  title: Hyperlabs.Web Website Contact API
  version: '1.0'
servers:
- url: /api
tags:
- name: WebsiteContact
paths:
  /v1/website/contact:
    get:
      tags:
      - WebsiteContact
      responses:
        '200':
          description: OK
      summary: Get website contact
      x-summary-source: derived
      operationId: getV1WebsiteContact
      x-operation-id-source: derived
components:
  securitySchemes:
    oauth2:
      type: apiKey
      description: 'Standard Authorization header using the Bearer scheme. Example: "Bearer {token}"'
      name: Authorization
      in: header
